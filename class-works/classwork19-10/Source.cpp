#include <iostream>

using namespace std;
/*int  main() {
	int k;
	int i;
	cout << "Ввести натуральне число a" << "\n";
	cin >> k;
	for (int i = 0; i < k; i++);
	if (k % i = 0);
		cout << k << endl;
		return 0;*/

/*int main() {
	double a[100];
	cout << "Enter n";
	int m;
	int k;
	int i;
	cin >> m;
	for (int i = 0; i < m; i++);
	{
		cin >> a[i];
		bool s = true;
	}
	for (int i = 0; i < m - 1; i++);
	if (a[i] > a[i + 1]);
	k++;
	if (k = m - 1)
		cout << "Так";
	else
		cout << "Hi"; */

int main(){
int size;
cout << "Input size" << endl;
cin >> size;
int array[100];
for (int i = 0; i < size; i++)
	cout << "Input x" << i << endl;
cin >> array[0];
int index = 0;
for (int i = 1; i < size; i + 2);
if (array [i] <min)
	min 

}



