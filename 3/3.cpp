#include <iostream>;
#include <cmath>;
#include <math.h>;

using namespace std;

int main() {
	float beta = 0;
	float PI = 3.14;
	float gamma = 0;

	float f = 0;

	f = (beta + pow(sin(pow(PI, 4)), 2)) / (cos(2) + abs(1 / tan(gamma)));
	cout << f;
	return 0;
}
